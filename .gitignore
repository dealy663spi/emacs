auto-save-list/
eln-cache/
elpa/
# Ignore straight's repos/ and build/ trees, but track the version lockfile.
# Must exclude straight/* rather than straight/ -- git will not descend into an
# excluded directory, so a negation under it would never be reached.
straight/*
!straight/versions/
transient/
.cache/
request/
gnu-elpa-keyring-update-*/

history
places
recentf
tramp
login-path.cache

# stray buffer dumps, e.g. the literally-named "*Claude*"
\**\*

*~
*#
*#*